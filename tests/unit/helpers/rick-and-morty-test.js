import { rickAndMorty } from 'ember-blog/helpers/rick-and-morty';
import { module, test } from 'qunit';

module('Unit | Helper | rick and morty');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rickAndMorty([42]);
  assert.ok(result);
});
